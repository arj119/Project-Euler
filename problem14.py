orgin = int(input('starting number: ')
            
            
if origin > 1:
            if origin % 2 == 0
           origin = origin/2
            print(origin)
        else:
            origin = (3*origin)+1
            print(origin)
            
            
            
        
